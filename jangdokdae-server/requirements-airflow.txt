# 장독대 Airflow 이미지 런타임 의존성 — 코어 파이프라인만(설계 00 §12.3)
#
# 포함: DB·수집·전처리·임베딩·클러스터링 등 DAG Task가 실제 import하는 것.
# 제외: fastapi·uvicorn(웹앱)·python-jose·passlib(인증) — DAG 런타임에 불필요.
# 버전은 pyproject.toml과 일치시킨다.
#
# 2026-06-22: bake-off로 운영 임베딩 모델을 ko-sroberta(HuggingFace/torch)로 확정 →
# sentence-transformers·langchain-huggingface 포함(torch ~2GB, 이미지 커짐 감수). gemini로
# 되돌리려면 EMBED_MODEL을 gemini-embedding-001로 바꾸고 이 둘을 제거하면 된다.

# DB / ORM / 마이그레이션
sqlalchemy==2.0.50
asyncpg==0.31.0
psycopg2-binary==2.9.12
pgvector==0.4.2
greenlet==3.5.1
alembic==1.18.4

# 설정
pydantic==2.13.4
pydantic-settings==2.14.1
pyyaml==6.0.3
python-dateutil==2.9.0.post0

# 수집 / 전처리
feedparser==6.0.12
httpx==0.28.1
trafilatura>=2.1.0
finance-datareader==0.9.202
pykrx>=1.2.8

# 임베딩 / LLM
langchain==1.3.4
langchain-google-vertexai==3.2.3
langchain-google-genai>=4.2.4
langgraph==1.2.4
# 운영 임베딩 모델 ko-sroberta(HuggingFace) — torch를 끌어온다(이미지 ~2GB↑).
langchain-huggingface>=1.2.2
sentence-transformers>=5.5.1

# 클러스터링
hdbscan>=0.8.44
scikit-learn>=1.9.0

# ExternalPythonOperator가 venv에서 context 직렬화 시 참조 — 없으면 무해한 경고만 남는다
pendulum>=3.0

